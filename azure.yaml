name: whisper-transcriber
metadata:
  template: whisper-transcriber@1.0.0
services:
  backend:
    project: ./backend
    language: js
    host: appservice
  frontend:
    project: ./frontend
    language: js
    host: staticwebapp
    dist: dist
